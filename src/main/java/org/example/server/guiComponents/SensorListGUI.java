package org.example.server.guiComponents;

import org.example.server.GUIHandler;

import javax.swing.*;
import java.awt.*;

public class SensorListGUI extends JPanel {

    public SensorListGUI(GUIHandler handler) {
        setBackground(Color.ORANGE);
    }
}
